# RelayLink 验证状态

文档 ID：TST-001\
状态：Active\
版本：v1.23.1\
更新日期：2026-09-20

本文记录当前仓库可重复执行的验证证据，不替代 [需求文档](../requirements/requirements.md) 的验收标准，也不把未在目标环境运行的项目标记为通过。

2026-09-20 增加 macOS Agent 双架构交付：发布脚本可生成 `osx-x64`（Intel）和 `osx-arm64`（Apple Silicon）自包含 Agent，tag 发行流水线新增两个压缩包并将资产完整性检查从四项调整为六项；新增独立低权限账号运行的 launchd 模板、部署说明和 ADR-0015。[GitHub Actions 运行 35501952662](https://github.com/DeronQi/RelayLink/actions/runs/35501952662)在 `macos-15-intel` 与 Apple Silicon `macos-15` 托管虚拟机上分别完成原生验收：两边启用端到端 TLS 的集成测试均为 26/26；两个自包含程序的 Mach-O 架构、配置检查、专用服务账号、程序/状态/配置/plist 权限、launchd 启动、异常退出恢复、本机只读状态页和端到端身份复用均通过。验收过程中实际发现并修复 macOS 不支持 `EphemeralKeySet` 加载 PKCS#12 身份，以及 PowerShell 发布脚本反斜杠路径导致 macOS 输出目录嵌套的问题；同时收紧客户端重新启用用例，使其等待配置确认后的真实可用性。该轮 Ubuntu、Windows 和前端既有检查也全部通过。GitHub 托管虚拟机无法覆盖系统重启后的开机自启、真实睡眠/唤醒、浏览器下载 quarantine、Developer ID 签名和 Apple 公证，因此 A44–A45 仍保留这些实机/正式发行验收项。

2026-09-18 按[本地多场景验收方案](local-acceptance-plan.md)新增隔离脚本，现场启动 1 个本地服务端、2 个 Agent 和 3 个模拟目标：3 条通道各 1 条 64 KiB 与各 8 条 1 MiB 并发随机流精确回显、标记隔离及半关闭通过，错误控制/数据首帧被拒绝，三个客户端/通道组合均有 SQLite 分钟样本；当前实例仍运行。随后真实进程集成测试 24/24、单元 36/36、前端组件 6/6 通过，匿名审计查询返回 401。运行目录、端口、停止方式和未覆盖项详见该方案；Docker SQL、跨主机、安装包升级及长时容量验收未在本轮执行。

## 已执行的仓库内检查

2026-09-20 修复管理台每 5 秒对每个客户端分别读取 channels 和 mappings 造成的 N+1 请求风暴：新增分页聚合快照 `GET /api/v1/dashboard/snapshot`，单页同时返回总览、客户端、通道指标和入口运行地址；原细粒度接口保持兼容。前端后续分页改为顺序读取，并使用单飞保护阻止慢请求与下一个轮询周期重叠。真实 Server 进程测试验证聚合结构、分页上限及响应不含客户端密钥/访问密钥；前端组件测试断言管理台只请求聚合快照、不再读取每客户端子资源。Windows .NET 10 预览 SDK 下 Release 构建通过，单元测试 36/36、启用端到端 TLS 的集成测试 26/26、前端组件测试 6/6、TypeScript/Vite 构建和 Prettier 检查通过。该版本已部署到测试服务器 `150.158.6.249`：配置预检、systemd 重启和 `/health/ready` 通过，4/4 Agent 自动重连；经本机互访入口读取聚合快照返回 4 个客户端及其通道/入口，现场请求约 14 ms。刷新内置浏览器后，服务端最近轮询日志只出现会话与聚合快照接口，不再出现按客户端读取 channels/mappings。旧 Server 程序目录已保留为回滚备份，上传和本地临时发布物已清理。尚未执行 100 客户端/500 通道的 P95 性能验收。

2026-09-20 增加 Linux Agent 正式交付路径：Agent 注册 systemd 生命周期，发布脚本和发行流水线生成 `linux-x64` 自包含压缩包，并新增受限服务账号的 systemd 模板。Windows .NET 10 预览 SDK 下解决方案 Release 构建、单元测试 36/36、启用端到端 TLS 的集成测试 25/25 通过；新增用例使用标准授权互访通道把目标 Agent 指向 Server 自身 `127.0.0.1` 管理端口，再从访问方 loopback 请求管理 API。`linux-x64` 自包含发布成功，并在 WSL Ubuntu 中实际启动后完成脱敏配置检查。

同日将该自包含 Linux Agent 安装到测试服务器 `150.158.6.249`：程序由 root 管理，服务以无登录账号 `relaylink-agent` 运行，独立状态目录 `/var/lib/relaylink-agent` 为 `0700`、配置为 `0600`，systemd 已启用并处于 active/running。创建标准客户端 `server-local` 和仅授权客户端互访的 `server-dashboard` 通道，目标为 Server loopback `127.0.0.1:18080`；只为本机客户端 `795s7` 创建访问映射。本机 Agent 分配 `127.0.0.1:32330`，经该入口请求 `/health/ready`、管理首页及概览 API 均成功，Codex 内置浏览器实际显示 4/4 客户端在线及该入口“已监听”。重启 Linux systemd 服务后 Agent 自动上线、端到端身份指纹保持不变、同一本机入口恢复并再次返回 ready。安装阶段发现 Server 的 `/var/lib/relaylink` 为专用账号 `0750`，因此 Agent 状态目录最终独立为 `/var/lib/relaylink-agent`，未放宽 Server 目录权限；上传的配置和安装包暂存文件及错误目录中的重复配置均已清理。尚未执行断网期间连接释放、未授权真实客户端负向访问及长时压力验收，因此 A42–A43 仍为部分通过。

2026-09-20 将当前最新版 Server 部署到测试服务器 `150.158.6.249`。首次切换暴露 Ubuntu 20.04 的 glibc 2.31 无法加载发布物中捆绑 SQLite 的问题，因此 Linux 发布改为通过 `SQLitePCLRaw.provider.sqlite3` 使用系统 `libsqlite3.so.0`，并从 linux-x64 发布目录剔除未使用的 `libe_sqlite3.so`。三份旧客户端配置已在保留逐文件备份后迁移到客户端级指纹，服务端配置预检和自动回滚均生效。随后发现旧 Agent 会把已移除的通道级证书字段纳入配置摘要；曾短暂在服务端恢复该字段以保持 3/3 旧 Agent 在线，但两台 Agent 更新后又因新版摘要不包含该字段而无法确认配置。按升级决策，服务端最终移除兼容字段：`795s7` 与 `qicl-mi` 两台新版 Agent 已重新上线，尚未更新的 `qicl-l7000k` 按预期离线，需升级后才能上线。服务在 7443/7444 与本机 18080 正常监听，`/health/ready` 返回 ready，端口建议接口路由存在且匿名返回 401，部署后无 warning/error。Windows .NET 10 预览 SDK 下单元测试 36/36 通过；尚未在该测试服务器执行真实通道字节流或数据库业务回归。

2026-09-20 修复 Agent 安装包在全新计算机启动时于 `InitializeWizard` 过早展开 `{app}`、导致安装目录尚未初始化便报错的问题；已有安装检测改用该阶段可用的 `{autopf}` 默认目录，并继续优先检查 `RelayLinkAgent` 服务。使用 Inno Setup 6.7.3 和本机 .NET 10 预览 SDK 成功生成 win-x64 自包含测试安装包 `0.0.0-regression`，`git diff --check` 通过。当前构建机已有 Agent 服务和默认目录，未能在完全无安装痕迹的管理员环境执行向导，因此仍需在干净 Windows 虚拟机或目标机完成首次安装实测。

2026-09-18 审计日志接入：服务端 SQLite 持久化登录成功/失败/注销、Agent 会话、普通代理及端到端连接生命周期元数据；成功登录和业务连接在审计起始事件写入确认后才放行。管理员查询接口支持范围、类型、客户端和分页，匿名拒绝。仓库测试覆盖登录事件脱敏、普通/端到端连接同 ID 起止查询、数据库锁冲突时拒绝登录和普通代理 Start、重启补记未闭合连接；页面支持管理员弹窗查看。隔离输出测试为单元 36/36、启用端到端 TLS 的集成测试 24/24、前端 6/6。运行中的本机/远端服务尚未更新；磁盘满、只读目录、高并发背压与真实部署待验收。

2026-09-18 当前连接查看与定向断开：服务端真实进程集成测试覆盖普通代理连接匿名列表、连接 ID 对应控制通道 Open、匿名/缺 CSRF 拒绝、错误通道返回 404、管理员断开后调用方 EOF 及列表清空；端到端测试覆盖已建立加密连接列表及管理员断开。隔离输出测试为单元 34/34、集成 `EnablePeerTlsTests=true` 21/21；前端组件测试 5/5，构建通过。运行中实例未更新，尚未执行页面手工验收。

2026-09-18 应用层安全组：服务端单元测试覆盖 IPv4/IPv6 单地址与 CIDR 匹配、默认开放、错误网段及引用拒绝；真实进程集成测试覆盖匿名/缺 CSRF 拒绝、创建与持久化、普通通道绑定、引用中删除拒绝、允许来源发起 Open、收紧规则撤销待建立连接及新来源拒绝、解除绑定后删除。隔离输出测试为单元 34/34、集成 `EnablePeerTlsTests=true` 19/19；前端测试 4/4、构建和格式检查通过。尚未部署到本机或远端运行实例；未对真实 NAT/IPv6 入口做部署验收。

2026-09-18 客户端禁用及安全撤销：单元测试验证被访问客户端禁用后，其他客户端的已配置入口仍通过结构和授权校验，密钥不一致仍被拒绝；真实进程集成测试验证管理员禁用被引用的客户端可保存完整客户端/入口 JSON，活动端到端连接立即断开、新请求被拒、重新启用后恢复通信；普通代理测试验证禁用后活动连接结束、云端监听停止及新 Agent 认证拒绝。隔离输出测试：单元 33/33、集成 `EnablePeerTlsTests=true` 18/18 通过。运行中的本机或远端实例未更新。

2026-09-18 匿名访问入口表格：真实服务进程集成测试对无需登录的 `GET /api/v1/clients/{id}/mappings` 验证入口/目标元数据可见、未知客户端返回 404，且响应不含访问密钥或目标证书指纹；管理写接口维持原鉴权。`EnablePeerTlsTests=true` 集成测试 16/16 通过，服务端隔离输出构建通过。前端组件测试验证匿名入口表格、本机地址和无删除按钮，`pnpm test` 3/3、`pnpm build`、`pnpm format:check` 通过。运行中的服务端与浏览器页面未更新，尚未做部署后目视验收。

2026-09-18 客户端级端到端证书指纹：单元测试覆盖授权通道无需指纹、客户端指纹格式校验、旧通道字段直接拒绝加载（不兼容、不迁移）；真实服务进程集成测试覆盖认证注册并确认配置后写入客户端 JSON、原通道不再带指纹、更换指纹注册被拒且原值保持，互访 TLS 全套集成仍可连通。使用隔离输出目录（避免覆盖正在运行的本机实例）执行单元测试 32/32、集成测试 `EnablePeerTlsTests=true` 16/16；前端 `pnpm build`、`pnpm test` 3/3、`pnpm format:check` 与 `git diff --check` 通过。未更新本机或远端运行中的 Server/Agent，也未执行浏览器端实时验收。仓库忽略目录 `.local` 内仍有一份旧字段验收配置，未自动改写。

2026-09-18 新增通道端口建议：单元测试覆盖从 19000 开始、跳过服务端端口/普通通道已分配端口/探测不可绑定端口（授权通道不占云端端口）；真实服务进程集成测试验证接口仅管理员可用、OS 新占用端口后建议值变化、用旧建议值保存返回 400 且不写入通道。前端组件测试验证先填 19001、创建后再次打开填 19002。Release .NET 测试为单元 31/31、集成 15/15；前端 `pnpm test` 3/3、`pnpm build`、`pnpm format:check` 和 `git diff --check` 通过。运行中的本机和远端服务尚未更新。

2026-09-18 SQLite 分钟流量：单元测试验证普通与端到端密文字节在同一 UTC 分钟聚合、重复采样不重计、通道删除后仍采集已有计数、90 天清理、旧 JSONL 只导入一次且原件保留。真实进程双 Agent、多授权通道的 16 条并发加密传输后，历史 API 对两个通道分别返回端到端密文字节且普通字节为零。`dotnet test RelayLink.slnx -c Release --no-restore -p:EnablePeerTlsTests=true -p:SkipAdminWebBuild=true` 通过：单元测试 29/29、集成测试 14/14；前端 `pnpm test` 3/3、`pnpm build`、`pnpm format:check` 通过。审计日志目前只完成调研与设计，未实现、未验收；本机和远端运行实例也尚未更新。此前表格中的 JSONL/累计值结果保留为历史测试记录，不代表当前实现。

2026-09-18 端到端访问入口调整：前端测试验证目标选定后只读展示自动生成的入口 ID、创建请求只发送两个目标 ID、无编辑按钮；真实进程集成测试验证新 ID 下发并完成双向数据通信、重复创建返回 400、PUT 修改返回 405、删除旧 ID 的入口会断开对应存量连接且不影响另一入口。Release 配置下 `dotnet test RelayLink.slnx -c Release --no-restore -p:EnablePeerTlsTests=true -p:SkipAdminWebBuild=true` 为单元测试 27/27、集成测试 14/14 通过；前端 `pnpm test` 3/3、`pnpm build` 和 `pnpm format:check` 通过。仍未重新部署已运行的本机或云端服务。

2026-09-18 通道配置变更撤销连接：真实进程集成测试验证普通代理通道的相同内容重复保存不断开、显示名修改/禁用/删除后断开存量连接；端到端访问验证相同内容保存不断开、目标通道修改或访问方入口删除后仅断开对应连接，另一条通道仍可完整回传。尚未对目标授权通道的删除和多访问方竞态单独做真实进程验收。

运行 `scripts/test.ps1` 可执行依赖还原、Release .NET 测试、前端 Vitest/格式检查及 `git diff --check`。当前结果为：

| 项目 | 结果 | 覆盖范围 |
|---|---|---|
| 单元测试 | 通过 | NTP1 分帧、非法 DATA、帧往返、部分帧期限、异常方向中止、配置摘要、重复 JSON、证书路径、一次性令牌、Agent 本地通道拒绝 |
| 本地 TLS/TCP 集成测试 | 跳过 | 测试代码覆盖服务端进程、注册、配置确认、Bind、Start 与字节回显；当前 Windows 沙箱的 Schannel 无法创建客户端凭据，未执行该测试 |
| 格式检查 | 通过 | `git diff --check` |
| 本地纯 TCP 多通道隔离 | 通过 | 三个通道各以不同响应标识回显；每通道 3 条并发、每条 1 MiB，调用方同时校验目标标识和随机载荷，未发现串流 |
| 本地纯 TCP 大流量与配额 | 通过 | 单通道 5 条并发、每条 2 MiB；三个通道各 3 条并发、每条 4 MiB；超出单通道上限时 6 条中 5 条成功、1 条受控拒绝 |
| 流量历史持久化与查询 | 通过 | 两秒采样写入本地 JSONL；2 条并发、每条 64 KiB 回显后，历史接口返回 echo 通道累计上行 131,072 字节、下行 131,092 字节及 2 条连接计数 |
| 本地纯 TCP 多客户端并发 | 通过 | 两个独立密钥客户端、五条具唯一目标标识的通道同时传输 7 条访问连接、每条 1 MiB；所有调用方均完成标识和随机载荷校验，历史样本分别归属两个客户端 |
| 独立管理前端构建与托管 | 通过 | React/TypeScript 类型检查、Prettier 检查通过；Server Release 发布连续两次成功，独立发布目录包含 `wwwroot/index.html`、`assets/app.js`、`assets/index.css`；发布物在 `127.0.0.1:18080` 提供页面与 JS |
| 管理页面匿名浏览 | 通过 | 浏览器确认两个客户端、五条通道、登录入口、搜索过滤、按通道历史流量弹窗；未登录客户端新增请求返回 401 |
| 管理 API 与配置下发 | 通过 | 隔离测试实例上完成登录、客户端创建/编辑/禁用/启用、Agent 配置下载、通道创建/编辑/禁用/删除；缺少 CSRF 的已登录写入返回 401。在线 Agent 收到目标端口更新后，3 条各 4 KiB 带标识模拟调用成功；禁用后监听端口关闭，历史样本记录目标方向 12,288 字节、访问方方向 12,294 字节 |
| 管理前端组件回归 | 通过 | Vitest + Testing Library 2 项测试覆盖匿名只读与历史图表、登录态切换、客户端与通道创建、通道禁用与删除，以及管理写请求携带 CSRF；TypeScript 与 Prettier 检查通过 |

2026-09-17 在 WSL Linux `dev_a` 的 .NET 10.0.100 稳定 SDK 上，以 `-p:EnablePeerTlsTests=true -p:SkipAdminWebBuild=true` 显式运行 Agent 互访集成测试，6 项通过、1 项旧 TLS 测试按原设置跳过；单元测试单独运行 15/15 通过。Windows 上先发现 Schannel 不支持从 PEM/PFX 直接使用临时私钥及 TLS `close_notify` 半关闭差异；修复文件私钥运行时导入和 TLS 内业务 EOF 后，以本机 Windows .NET 10 预览 SDK 重跑互访集成测试，6 项通过、1 项旧 TLS 测试跳过，单元测试 15/15 通过。Windows 目标机仍需用稳定运行时实机复验。

互访集成覆盖：双 Agent 外层 TLS 注册、端到端证书固定和访问密钥挑战、128 KiB 随机字节精确回显及半关闭；双通道 16 并发连接、每条 1 MiB 且回包目标标识正确；无映射的已注册客户端和错误 HMAC 证明被拒绝、被访问目标零连接；匿名云端代理端口关闭且 API 不泄漏访问密钥；管理端登录/CSRF 后新增映射自动下发至在线 Agent 并实际连通。并发测试还确认服务端普通业务流量保持 0，互访密文计数各方向大于实际有效载荷（TLS 开销），不混入普通历史采样。前端 TypeScript 构建、3 项组件测试及 Prettier 检查通过。

新增 Agent 自选端口后，Windows 预览版与 Linux 10.0.100 稳定版最终回归均为单元测试 17/17、互访集成 7/7（另 1 项旧 TLS 用例按原设置跳过）；集成包含真实 Agent 进程重启、旧端口占用、新端口连通、服务端状态上报和 Agent loopback 网页只读内容。前端构建、3 项组件测试及格式检查通过。
Windows Server 的 Service 生命周期、端口绑定及证书私钥权限尚未在目标主机执行实机验证。

2026-09-17 的 Agent 配置内嵌 CA 改动：单元测试 20/20、管理前端组件测试 3/3、TypeScript/Vite 构建及 Prettier 检查通过。单元测试覆盖服务端生成含 Base64 CA 的配置并由 Agent 无旁路文件加载、无效 Base64 拒绝，以及旧 PEM 路径兼容和双重信任来源拒绝。win-x64 自包含 Agent 和 Inno Setup 6.7.3 安装包 `1.0.1` 编译成功；尚未在目标 Windows 主机执行安装。Linux 服务端已在当前云主机完成配置检查、部署与重启，SSH 转发页面及就绪检查返回 200，新页面不再显示 CA 路径字段。当前 Windows 预览 SDK 下完整集成测试仍在 `TunnelAcceptorService.LoadServerCertificate` 的测试 PFX 导入处失败（1 失败、2 跳过），因此不能把本次完整集成测试标记为通过；需在稳定运行时或 Linux 环境再跑端到端互访。

同日修复创建客户端时大写 ID 触发的笼统 `dashboard update` 错误：表单将大写字母转为小写并限制 ID 格式，服务端对非法 ID 返回具体规则。单元测试 21/21、前端组件测试 3/3、构建和格式检查通过；Linux 服务端重新部署后，经 SSH 转发的页面及就绪检查均返回 200，新前端资源包含 ID 转小写提示。未在生产环境创建额外测试客户端。

Agent 安装包新增桌面监控快捷方式后，使用 Windows PowerShell 5.1 对默认端口 18081、自定义端口、`dashboardPort=0`、卸载移除和同名快捷方式冲突保护进行本地检查；Inno Setup 6.7.3 已成功编译 win-x64 安装包 `1.0.3`，发布目录未发现 Agent 配置或证书/私钥文件，单元测试 21/21 通过。尚未在管理员会话中执行完整安装、桌面双击与卸载，因此 A27 仅完成本地脚本和构建验证。

同日对云端普通通道的 RDP 小包延迟做现场验证：Windows Agent 主机向本机 `127.0.0.1:3389` 发送无凭据 RDP 协商请求，5 次响应约 6–20 ms；经 `150.158.6.249:19000` 转发，修复前 5 次约 295–317 ms，服务端单侧启用 `TCP_NODELAY` 后约 131–160 ms，服务端与 Agent 均更新后约 74–88 ms。每次均收到 19 字节协商响应。服务端重新发布、配置校验和健康检查通过；本机 Windows Service 更新 Agent DLL、重启后显示在线。该测试只覆盖 TCP 建连、隧道建立和 RDP 初始协商，不包含账号登录、图形传输或持续操作；登录后黑屏是否消除仍待实际 mstsc 复验。此次 Windows 预览 SDK 单元测试 23/23 通过，完整 TLS 集成测试仍受测试 PFX 导入失败限制。

2026-09-18 按 [ADR-0008](../adr/0008-separated-control-and-raw-data.md) 分离控制与数据入口并改造普通代理为原始 TCP 双向复制；`TargetReady/Start` 改走控制连接，帧版本升为 2 以拒绝旧版本。`dotnet build RelayLink.slnx --no-restore` 通过，单元测试 26/26 通过（含旧版本拒绝、控制会话结束取消数据寿命及数据端口冲突）；`ServerTunnelTests` 2/2 通过，覆盖正确绑定后的原始字节、双向半关闭，以及未认证数据绑定和跨入口错误首帧拒绝。在允许 Windows 当前用户证书容器访问的本机环境以 `-p:EnablePeerTlsTests=true` 运行完整集成测试 9/9 通过；包括互访两通道 16 并发、每条 1 MiB 随机字节、128 KiB 半关闭、错误访问证明拒绝及动态配置下发。沙箱内同一互访测试因证书私钥 PFX 导入权限失败，故此结论仅对应上述本机运行条件。

使用 `scripts/start-local-control-data-smoke.ps1` 启动本机 Server、Agent、模拟目标并运行模拟调用方；控制和数据端口仅绑定 `127.0.0.1`，未认证绑定及错误首帧均被拒绝，32 路并发每路 1 MiB 随机字节、发送方向半关闭与回包 EOF 全部通过。该结果不等于跨公网 RDP 登录显示恢复，也不验证普通数据通道的链路机密性：它目前是明文 TCP；旧版 Server/Agent 不兼容新的双端口部署。

2026-09-18 已将独立控制/数据端口版本部署到 `150.158.6.249` 和本机 Windows Service，云端 7443/7444/19000 监听、客户端在线、通道可用；无凭据的 RDP 初始握手经代理与本机直连均收到相同的 19 字节响应。用户随后实测登录仍然黑屏，因此不能把跨公网 RDP 验收标记为通过。为继续定位，普通代理两端新增按连接 ID 的建连阶段、10 秒双向字节与空闲时间、单向 EOF/错误及最终结果日志；Agent 日志落在受保护的配置目录并按 4 MiB 轮换。新增日志文件单元测试后单元测试 27/27、默认集成测试 3/3 通过，另 1 项互访测试按原设置跳过；完整图形登录仍需在两端诊断版部署后复测。

两端诊断版部署后，用户确认公网访问端 `116.235.78.200` 在 09:04–09:05 的连接为本人复测：连接 ID `63247221-a4fa-49ed-a248-12a15e81b268` 的服务端与 Agent 日志显示，约 40 秒时访问端→目标约 19 MiB，目标→访问端仅约 33 KiB；约 50 秒时反向增至约 382 KiB，80 秒时约 568 KiB，两端计数相符。目标 Windows RDP 事件于 09:04:13 记录会话重连成功、等待图形子系统；09:04:56 记录多传输通道因未收到 UDP 包而改用 TCP，距登录约 43 秒。09:05:38 的下一次重连也在 09:06:21 出现相同的约 43 秒 UDP 等待。此前 08:37 的内网直连记录为客户端不支持 UDP、立即走 TCP。用户补充黑屏一段时间后画面会恢复；这些时间关联支持“访问端尝试 UDP、超时后 TCP 回退造成主要等待”的假设，但尚未通过访问端关闭 UDP 的 A/B 复测确认。期间服务端接收队列曾积压约 2.8 MiB，说明还需观察回退后的背压；RDP 目标日志中其他连接的密码错误事件不能归因于本次成功重连。完整图形交互和时延验收仍未通过。

2026-09-18 使用 `scripts/run-local-database-acceptance.ps1` 在 Windows Docker Desktop 启动 SQL Server 2022 和 PostgreSQL 17 容器，以随机本地端口映射到仅监听 loopback 的目标端口；另启动一套本机 Server/Agent，分别配置 MSSQL 与 PostgreSQL 两条独立 TCP 通道。通过代理端口使用 SQLCMD 和容器内 `psql` 充当调用方，两个数据库均完成建表与三行种子数据插入；`SELECT COUNT(*)=3`、客户/订单联表分组得到 `Ana=30、Bo=7`、`UPDATE` 后总额 `39`、事务 `ROLLBACK` 后仍为 `39`、临时 `INSERT` 后总额 `50`、`DELETE` 后行数回到 `3`，全部断言通过。验收后 Server/Agent 仍运行、两容器仍启动，仪表盘显示 1 个在线客户端、2 条可用通道、双向字节数非零。本次是本机明文 RelayLink 通道功能验证，不覆盖 SQL TLS、跨公网、连接池、事务中断或大结果集。脚本每次生成独立配置、随机凭据和端口，运行状态及测试凭据分别位于被忽略的 `.local/database-acceptance/<运行 ID>/state.json` 和 `test-credentials.json`，不要将后者提交到仓库。

同日使用 `scripts/run-local-database-peer-acceptance.ps1` 复用上述数据库容器和数据，另启一套本机 Server、被访问 Agent 与访问方 Agent。为该隔离环境生成一次性控制 TLS CA/服务端证书、被访问 Agent 身份证书，以及两条通道各自独立的 32 字节访问密钥；被访问 Agent 的证书 SHA-256 指纹固定在服务端通道与访问方映射中。通过访问方自选的两个 `127.0.0.1` 入口，SQLCMD 与 `psql` 分别完成 MSSQL、PostgreSQL 的行数/总额 `3/39`、联表分组 `Ana=30、Bo=9`、事务内临时插入得到 4 行并回滚到 3 行，全部断言通过。目标通道无匿名云端监听；服务端普通业务字节统计为 0，互访密文方向统计分别为 10,411/14,770 字节。另以 `-p:EnablePeerTlsTests=true` 运行 `PeerConnectionTests`，7/7 通过，覆盖内层 TLS 与半关闭、双通道并发、未授权映射与错误访问证明拒绝、在线下发及端口轮换。该结果证明本机互访路径及 SQL 通信可用，不等同于 SQL 协议自身 TLS 或跨主机链路验收；本机 .NET SDK 为 10.0.200 预览版，正式运行环境仍需稳定版复验。脚本输出本地状态文件位于被忽略的 `.local/database-peer-acceptance/<运行 ID>/state.json`。

2026-09-19 用户报告两处真实环境投产：跨公网 RDP 已在自有服务器 `150.158.6.249` 上连续运行一天，公网访问端经隧道登录内网 Windows 桌面并按真实使用方式使用；SQL Server 数据通道已在另一环境完成部署并投入生产。这两项说明普通代理通道的跨公网、跨 NAT 路径已在真实负载下可用，不再是仅有本机证据的功能。需要同时记下的边界：连续运行一天属于真实场景下的稳定性观察，**不构成受控容量验收**——A16 的 24 小时满载多发压测仍未执行，需求文档中的 100 客户端 / 500 通道 / 1000 连接 / 100 Mbps 仍是拟定基线；上文记录的“RDP 登录后数十秒才出画面”现象的根因仍未通过访问端关闭 UDP 的 A/B 复测确认，只能记为“生产可用、根因未定位”，不得写成已解决。

## 需求验收状态

| 验收项 | 当前状态 | 所需后续证据 |
|---|---|---|
| A01–A06（认证、会话、配置） | 部分由单元测试覆盖 | 在真实 TLS 环境执行正/负配置与同 ID 会话测试 |
| A07–A09（字节流、半关闭、目标故障） | 本机纯 TCP 字节流及三通道隔离已验证 | Linux 或 Windows Server + Windows/Linux Agent 的真实 TLS 集成测试及半关闭、目标故障证据 |
| A10–A14（恢复、令牌竞争、配额与慢读） | 未验收 | 故障注入、并发/慢读测试与资源计数检查 |
| A15（仪表盘/API） | 本地独立前端、匿名只读、组件回归、管理 API、在线下发及历史图表已验证；全量轮询已改为分页聚合快照并覆盖无 N+1 请求回归 | 在部署环境核对 HTTPS/内网保护、会话过期及 100 客户端/500 通道页面 P95；真实浏览器中的登录后表单提交仍需部署环境回归 |
| A16（容量与 24 小时压测） | 未执行 | 按需求文档指定资源、RTT 和负载记录报告 |
| A17（安全组） | 未执行 | 云网络安全组和内外网端口实机验证 |
| A18–A22（Agent 安全互访） | 双 Agent 本机 Linux 与 Windows 集成通过；跨主机部分待验 | Windows 稳定版运行时及服务身份、两台真实内网主机经服务端中继、错误指纹/慢读/断线资源回收、长时压力与容量基线 |
| A23–A24（Agent 自选端口与本机网页） | Windows 和 Linux 单元与双 Agent 本机集成通过：首次分配、重启复用、占用轮换；网页显示地址且不含密钥，服务端上报断言通过；两平台均执行真实 Agent 进程重启、端口冲突轮换及新地址连通 | Windows Service 实际权限、跨主机及重启现场验证 |
| A25–A26（Windows Agent 安装包） | win-x64 自包含发布和 Inno Setup 6.7.3 安装包编译成功；PowerShell 安装/卸载脚本语法检查与 `git diff --check` 通过，发布目录未发现真实配置或证书 | 本机静默缺配置测试被 UAC 挡住，未进入安装阶段；需在管理员会话实测无配置/无效配置拒绝、有效配置安装后 Online、重启自启及卸载保留状态；当前构建机 SDK 为 .NET 10 预览版，正式发布需稳定版复验并签名安装包 |
| A27（桌面监控快捷方式） | Windows PowerShell 5.1 本地验证默认端口、自定义端口、状态页关闭、卸载移除和同名冲突保护；`1.0.3` 安装包编译成功 | 在目标 Windows 管理员会话中完整安装，双击确认由默认浏览器打开正确地址，并验证卸载清理 |
| A28–A29（独立控制/数据与原始流） | 本机错误入口/无授权令牌拒绝、正确绑定后原始字节、32 路每路 1 MiB 半关闭模拟通过；互访端到端实验 9/9 通过；云端与本机 Agent 已部署；跨公网 RDP 已在自有服务器连续运行一天（生产使用），登录后数十秒才出画面的现象根因未确认 | 访问端关闭 UDP 后 A/B 复测延迟与图形交互；SQL TLS、受信网络或网络层加密、持续大流量和资源压测 |
| A42–A43（Linux Agent 与管理页互访） | `linux-x64` 自包含发布、WSL 配置检查及真实进程集成测试通过；测试服务器已按最小文件权限安装 systemd 服务，重启后复用身份并自动上线；指定 Windows 客户端经标准授权互访入口访问 Server loopback 管理页，API 与内置浏览器实测通过 | 未授权真实客户端负向访问、断网/停止时连接与资源释放、长时压力及正式稳定版运行时复验 |
| A44–A45（macOS Agent 双架构） | GitHub 托管 Intel 与 Apple Silicon macOS 使用稳定版 .NET 10 原生运行：两边集成测试 26/26，双 RID 自包含架构、配置检查、低权限 launchd 启动与异常恢复、文件权限、状态页和身份复用通过 | 系统重启后的开机自启、真实睡眠/唤醒、浏览器下载 quarantine、物理 Mac 长时运行；Developer ID 签名、公证和 stapling |
| SQL Server / PostgreSQL 数据库代理验收 | 本机普通代理与双 Agent 端到端加密互访的常见 SQL、事务回滚通过；另有用户报告的另一环境 SQL Server 通道已投产 | 固定端口与 SQL 协议自身 TLS、跨公网、事务中断、大结果集及连接池场景 |

## 执行前置条件

- Linux 或 Windows 目标机满足 .NET 10 原生运行依赖，具备有效服务端证书及最小权限服务账号；自包含发布不代表可以忽略目标发行版与 OpenSSL 等系统依赖的兼容性验证。
- Windows Agent 目标机能够使用 Schannel 验证服务端证书链和名称。
- 云端内网、目标 SQL Server、固定 TCP 端口与安全组规则已经按需求配置。
- 压测环境必须记录 CPU、内存、NIC、运行时补丁、RTT、丢包、SQL 与驱动版本；没有这些条件的数字不得作为容量结论。
