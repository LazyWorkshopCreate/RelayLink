# RelayLink 仓库协作规则

## 开始工作

先阅读 README.md、docs/README.md 及与任务相关的需求、技术设计。遵循 docs/development/repository-layout.md 和 docs/development/documentation-policy.md。

## 范围与实现

- 使用 C# / .NET；项目和命名空间前缀为 RelayLink。
- 首期仅 TCP；Linux Server、Windows Agent；每客户端独立密钥与服务端配置下发。
- 网络 ACL 由安全组负责；保持设计中的 TLS、身份校验和资源限制。
- 通道不在 Agent 本地维护，不引入热更新、写入型仪表盘或 SQL 重放。
- 文档中的 Draft/Proposed 不是已批准或已实现的事实。遵从用户后续明确决策，改动时同步文档。
- 不自行扩展需求；影响既有架构的决定记录 ADR，普通修复无需单独 ADR。

## 代码与验证

- 采用异步 I/O、取消令牌、有界缓冲；关注半关闭、背压及资源恰好释放一次。
- 不提交真实密钥、证书私钥、数据库凭据、站点真实配置或业务载荷。
- 按改动执行必要检查；尚无项目时不得声称 build/test 已通过。
- 需求、设计、协议和配置变化应在同一提交或 PR 内同步文档与相关示例。
- 不覆盖用户未提交修改；不自动推送、发布或修改远程仓库配置。

## 文档

中文正文，稳定英文文件名，UTF-8、LF；链接使用仓库内相对路径。详细规则以文档管理规则为准。任务完成时说明实际改动、检查结果和未完成项。
